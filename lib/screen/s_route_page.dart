import 'package:flutter/material.dart';

class RoutePage extends StatelessWidget {
  static final String id = '/route_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(),
      ),
    );
  }
}
